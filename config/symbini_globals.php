<?php

//Symbini
global $DEFAULT_LANG;
global $DEFAULT_PROJ_ID;

global $DEFAULTCATID;
global $DEFAULT_TITLE;
global $EXTENDED_LANG;
global $TID_FOCUS;
global $ADMIN_EMAIL;
global $SYSTEM_EMAIL;
global $CHARSET;
global $PORTAL_GUID;
global $SECURITY_KEY;
global $SERVER_HOST;
global $CLIENT_ROOT;
global $SERVER_ROOT;
global $TEMP_DIR_ROOT;
global $LOG_PATH;

/*
//Path to CSS files
global $CSS_VERSION_RELEASE;
global $CSS_BASE_PATH;
 */

//Path to user uploaded images files.  Used by tinyMCE. This is NOT for collection images. See section immediatly below for collection image location
global $PUBLIC_IMAGE_UPLOAD_ROOT;

//the root for the collection image directory
global $IMAGE_DOMAIN;
global $IMAGE_ROOT_URL;
global $IMAGE_ROOT_PATH;

//Pixel width of web images
global $IMG_WEB_WIDTH;
global $IMG_TN_WIDTH;
global $IMG_LG_WIDTH;
global $IMG_FILE_SIZE_LIMIT;
global $IPLANT_IMAGE_IMPORT_PATH;

//$USE_IMAGE_MAGICK = 0;		//1 = ImageMagick resize images, given that it's installed (faster, less memory intensive)
global $TESSERACT_PATH;
global $NLP_LBCC_ACTIVATED;
global $NLP_SALIX_ACTIVATED;

//Module activations
global $OCCURRENCE_MOD_IS_ACTIVE;
global $FLORA_MOD_IS_ACTIVE;
global $KEY_MOD_IS_ACTIVE;

//Configurations for publishing to GBIF
global $GBIF_USERNAME;
global $GBIF_PASSWORD;
global $GBIF_ORG_KEY;

//Misc variables
global $DEFAULT_TAXON_SEARCH;

global $GOOGLE_MAP_KEY;
global $MAPBOX_API_KEY;
global $MAP_THUMBNAILS;

global $STORE_STATISTICS;
global $MAPPING_BOUNDARIES;
global $ACTIVATE_GEOLOCATION;
global $GOOGLE_ANALYTICS_KEY;
global $GOOGLE_ANALYTICS_TAG_ID;
global $RECAPTCHA_PUBLIC_KEY;
global $RECAPTCHA_PRIVATE_KEY;
global $TAXONOMIC_AUTHORITIES;
global $QUICK_HOST_ENTRY_IS_ACTIVE;
global $GLOSSARY_EXPORT_BANNER;
global $DYN_CHECKLIST_RADIUS;
global $DISPLAY_COMMON_NAMES;
global $ACTIVATE_DUPLICATES;
global $ACTIVATE_EXSICCATI;
global $ACTIVATE_GEOLOCATE_TOOLKIT;
global $SEARCH_BY_TRAITS;
global $CALENDAR_TRAIT_PLOTS;

global $IGSN_ACTIVATION;

global $RIGHTS_TERMS;
global $COOKIE_SECURE;

//Symbbase
global $CODE_VERSION;
global $USER_RIGHTS;
global $PARAMS_ARR;
global $CSS_VERSION;
global $USER_DISPLAY_NAME;
global $USERNAME;
global $SYMB_UID;
global $IS_ADMIN;
global $AVAILABLE_LANGS;
global $LANG_TAG;
?>
